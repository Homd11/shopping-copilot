{
  "name": "@shopping-copilot/panel",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite --host 127.0.0.1 --port 4100",
    "test": "vitest run --passWithNoTests --pool=threads --maxWorkers=1"
  },
  "devDependencies": {
    "@types/jsdom": "21.1.7",
    "jsdom": "26.1.0",
    "vite": "7.1.3",
    "vitest": "3.2.4"
  }
}
