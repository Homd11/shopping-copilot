{
  "name": "@shopping-copilot/bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run --passWithNoTests --pool=threads --maxWorkers=1"
  },
  "devDependencies": {
    "vitest": "3.2.4"
  }
}
